---
interface Props {
  platform: string;
  username?: string;
}
const { platform, username = "" } = Astro.props;
---

<a
  href={`https://www.${platform}.com/${username}`}
  class="bg-primary py-2 px-4 rounded text-white">{platform}</a
>
